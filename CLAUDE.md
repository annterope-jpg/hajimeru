# hajimeru / いま、はじめる

成人の着手困難に対する、日常のセルフマネジメント支援アプリ。
タスクの完了ではなく、**最初の行動を開始できた時点を成功として記録する**。

診断・治療・服薬判断を行う医療サービスではない。現在は本人・関係者によるUX確認用の試作版。

---

## 1. 作業ルール

- **mainに直接コミットしない。** 必ずブランチを切る（例: `fix/...`, `feat/...`）
- 変更を提案する前に必ず通す:
  ```
  pnpm typecheck && pnpm lint && pnpm test --run
  ```
- PRは `gh pr create --draft` で下書きとして作る
- コミットは論理的に1変更1コミット
- `src/domain/` は純粋TypeScript。React・DOM・ストレージ実装を import しない
- ドメイン層のロジックを変更したら、必ず対応するテストを追加・更新する

---

## 2. 実装しないもの（意図的な非機能）

以下は「便利そうだから」「親切だから」という理由で追加してはいけない。
すべて臨床的な根拠がある意図的な欠落であり、**追加すると害になる**。

### 数字による自己評価の禁止

- ❌ 開始率・達成率・完了率のパーセント表示
- ❌ 計画数と開始数の並置（隣り合う2つの数字は、ユーザーの頭の中で引き算される）
- ❌ ストリーク・連続日数
- ❌ カレンダー上の未実施日のマーキング
- ❌ 再訪を促す通知（「3日間使っていません」等）
- ❌ 「未達」を意味する赤色の使用

**理由:** このアプリの利用者は、長年の失敗履歴と自己批判を抱えている可能性が高い。
先延ばしは短期的な気分修復（情動調整）として機能しており、
失敗の可視化は次回の接近をより重くする。記録機能は容易に自己処罰の道具になる。

集計関数（`calculateInsightMetrics` の `startRate` / `plannedCount`）は
将来の分析用に**計算は残す**が、利用者向け画面には**表示しない**。

### 未来の負の帰結を想像させることの禁止

- ❌ 「やらなかったらどうなりますか」「困ることは何ですか」を入力させるUI

**理由:** エピソード的未来思考（EFT）は遅延割引を低減するが、
47研究のメタ分析では**ポジティブ価のEFTのみが有効**（Hedges' g = 0.64）で、
中性・ネガティブ価のEFTは g = -0.03 と無効だった。
「やらないと困ること」を想像させる設計は、善意で追加されやすく、効果を打ち消す。

### 診断的な出力の禁止

- ❌ 疾患名の示唆（「うつの可能性があります」等）
- ❌ ADHD等の診断への帰属（「ADHDの特性です」）
- ❌ スコアに基づく判定・重症度表示

医療機器該当性を避けるためであり、かつ利用者が自分に診断ラベルを貼る設計にしないため。

### 危機介入機能の禁止

希死念慮への対応機能を実装しない。中途半端な実装はかえって危険。
`src/domain/safety.ts` の分類ゲート（相談先の案内まで）を超えないこと。

---

## 3. アーキテクチャ

```
src/domain/     純粋TS。臨床ロジックの単一の置き場所。React/DOM非依存
src/data/       ローカル永続化・マイグレーション・リポジトリ
src/services/   AI・同期・通知・エクスポート（すべて任意機能）
app/            expo-router の画面
tests/          domain と data の単体テスト
```

`src/domain/` を独立させているのは、臨床判定ロジックが1箇所に集約され、
テスト可能で、読んで監査できる状態を保つため。UI改修が臨床ロジックに波及しないこと。

---

## 4. 着手困難のモデル（アセスメント7軸の根拠）

このアプリの7軸は、恣意的なリストではなく機序の分解に対応している。
**特に以下の3つは統合してはいけない。**介入が異なる。

| 軸 (`Bottleneck`) | 機序 | 主たる介入 |
|---|---|---|
| `taskClarity` | 実行機能負荷・意思決定過負荷 | 最初の1動作を30秒以内まで具体化 |
| `aversion` | **遅延嫌悪**（遅延・接近そのものへの否定的情動反応） | 嫌悪を下げずに開始する（曝露的接近） |
| `rewardDistance` | **遅延割引**（報酬が時間的に遠い） | 開始直後に見える変化を置く / EFT |
| `effortCost`※ | **労力割引**（コスト側の問題） | 摩擦の除去・判断回数の削減 |
| `lowActivation` | 低覚醒・CDS成分・概日位相 | 身体を起こす。認知的方略より先に |
| `cueWeakness` | 展望記憶・目標表象の減衰 | 外的な戻る目印・実行意図 |
| `competingReward` | 競合報酬・摩擦の非対称性 | 競合行動側の摩擦を上げる |
| `timeAmbiguity` | 時間表象 | 時刻ではなく出来事に紐づける |

※ `effortCost` は現在独立した軸として実装されていない。`aversion` に混ざっている。
将来分離を検討する価値がある（「イヤ」と「自分には重い」は別のもの）。

**遅延嫌悪 ≠ 遅延割引。**
前者は情動反応で獲得性、後者は割引パラメータ。
「報酬遅延障害」として一括りにすると介入を誤る。

**低覚醒は最優先の割り込み。**
低覚醒状態では認知的方略が機能しにくいため、他の軸より先に身体を起こす。
また `activationSource` で疲労（fatigue）と不安による凍りつき（freeze）を分けているのは、
必要な介入が違うため（前者は覚醒を上げる、後者は緊張を下げる）。

---

## 5. 出力設計の原則

### 入力と出力を対応させる

利用者が丁寧に答えた項目が、画面で最も目立つ出力に反映されなければ、
「答えた意味がなかった」という感覚が残る。これは信頼を損なう。

`firstAction` は `selectActionForBottlenecks` によって主ボトルネックに応じて選ばれる。
新しい提案候補を追加するときは、`rationaleTag` を正しく設定すること。

**現状の既知のギャップ:** 多くのカテゴリに `activate_body` と
`interrupt_competition` の候補がない。該当ボトルネックが主でも
フォールバックが起きる。候補を足す価値がある。

### コンパッションは文言ではなく構造で実装する

「大丈夫、少しずつでいいんですよ」のような優しい文言を足しても効かない。
うまくいかなかったとき「こんなに優しくされているのにできない自分」という
二重の恥を作ることすらある。

機能するのは以下:

1. **失敗を測定しない構造**（§2の禁止事項そのもの）
2. **帰属先を課題側に置く文言** —「あなたは悪くない」ではなく「その動作がまだ大きすぎます」
3. **正常化の説明** — 慰めではなく機序の説明として書く

### 因果を主張しない

集計から傾向を出すときは、必ず相関にとどめる。
`formatActivationTrend` の文言（「記録上の並び方であり、覚醒度が開始を変えたとは
判断できません」）が基準。サンプル数の下限（現在5組）も維持すること。

`hasHelpfulReflection` が**ふりかえり未実施の記録を証拠に数えない**設計も同様の理由。
頻度だけで「効いた」と言わない。

---

## 6. エビデンスの階層（機能追加の判断材料）

| 強度 | 該当するもの |
|---|---|
| 比較的強い | ADHD特異的CBT、環境調整、組織化・計画・時間管理の技能訓練 |
| 一定の根拠 | 実行意図（if-thenプランニング）、エピソード的未来思考（遅延割引に対して） |
| 理論的に合理的だがRCT不十分 | friction設計、微小報酬、2〜3分のスターター課題、body doubling |

下段の機能を実装するのは構わないが、
**効果を断定する文言を書かない**こと。

---

## 7. プライバシー

- ローカル完結が既定。AI・同期は任意機能で、無効のまま全機能が動くこと
- `src/domain/safety.ts` はマッチしたテキストを一切エコーしない（ログ混入防止）。この性質を壊さない
- 課題テキストは臨床データとして扱う。自由入力欄（`valueAnchor`, `competingAction`）は
  個人的な内容を含みうる。PII正規表現は構造化された情報しか捕まえないことを前提に設計する

---

## 8. 進行中の改修（2026年8月時点）

**完了**
- 開始率のパーセント表示を削除し、累積の開始回数のみに（`app/(tabs)/insights.tsx`）
- `firstAction` を主ボトルネックに応じて選択（`selectActionForBottlenecks`）

**次の候補（優先順）**
1. **EFTの導入** — `rewardDistance` が高いとき、ポジティブ価の未来場面を
   3スロット（いつ・どこ／目に入るもの／聞こえる音か体の感じ）で構成させる。
   現在の `microReward` は即時報酬の配置であって EFT ではない。両方あってよい
2. **不安・自責・凍りつきへの介入** — 「やらなければ」という焦りと自責・不安が
   課題手がかりに連合している状態を解きほぐす。CFT的には脅威システムの活性化。
   焦りは動機づけではなく、必要な実行機能を阻害する
3. **時間帯分布** — `createdAt` から算出可能。追加入力ゼロで概日位相の手がかりになる
4. 履歴の「計画」ドット（未開始の可視化）の再検討
5. 直近7日カウンター（ストリーク隣接構造）の再検討
6. 提案候補の `rationaleTag` カバレッジ拡充
